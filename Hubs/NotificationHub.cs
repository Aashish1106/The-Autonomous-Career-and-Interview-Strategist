using Microsoft.AspNetCore.SignalR;

namespace AutoJobStrategist.Api.Hubs
{
    // This class can be empty for now! 
    // It simply serves as the endpoint for the React client to connect to.
    public class NotificationHub : Hub
    {
    }
}